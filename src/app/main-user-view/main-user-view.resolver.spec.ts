import { TestBed } from '@angular/core/testing';
import { ResolveFn } from '@angular/router';

import { mainUserViewResolver } from './main-user-view.resolver';

describe('mainUserViewResolver', () => {});
